stages:
  - build
  - deploy

variables:
  DEPLOY_USER: "mori"
  DEPLOY_SERVER: "79.133.182.202"
  DEPLOY_PATH: "/home/mori/PTW/"

build:
  stage: build
  image: docker:latest
  before_script:
    - docker info
  script:
    - docker compose build



deploy:
  stage: deploy
  image: alpine:latest
  rules:
    - if: '$CI_COMMIT_BRANCH == "master"'
      when: always
  before_script:
    - apk add --no-cache openssh-client git bash
    - eval $(ssh-agent -s)
    - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -
    - mkdir -p ~/.ssh
    - chmod 700 ~/.ssh
    - ssh-keyscan -H $DEPLOY_SERVER >> ~/.ssh/known_hosts
  script:
    - ssh $DEPLOY_USER@$DEPLOY_SERVER "cd $DEPLOY_PATH && git pull && docker compose up -d"

  